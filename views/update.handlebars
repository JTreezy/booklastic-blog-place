<h2 style="font-size: 80px; padding: 20px;">{{first_name}}'s Library</h2>
    <!-- this is where the review input starts -->
    <div class="container">
        <h3 class="d-flex justify-content-center p-5">Your Review</h3>
        <div class="input-group mb-3">
            <div class="input-group-prepend">
              <span class="input-group-text">Review Title</span>
            </div>
                <textarea type="text" class="form-control" aria-label="Title" aria-describedby="basic-addon1" id="titleEl">{{title}}</textarea>
    </div>
        <!-- this is where the authore and title autopopulate-->
    <div class="container" id="autopopulatecontainer">
        <ul class="m-3 row">
            <li class="list-group-item disabled mb-2 col-8" id="autopopTitle">{{book.title}}</li>
            <li class="list-group-item disabled mb-2 col" id="autopopAuth">{{book.author}}</li>
        </ul>
    </div>
    {{!-- review comment --}}
    </div>
    <div class="container">
        <div class="form-group">
            <label for="comment">Review 📝</label>
            <textarea class="form-control" rows="5" id="reviewEl">{{review}}</textarea>
            <div class="d-flex justify-content-end my-4">
                <button type="button" class="btn btn-link editbtn" id="updatebtn" style="padding:10px;" value="{{id}}"><img src="/images/update.png" alt="update icon" style="width:50px;"></button>
            </div>
        </div>
    </div>
{{!-- 


<div class="container d-flex flex-wrap">
<div class="d-flex">
        <div class="px-1">
            <div class="card text-dark bg-warning mb-3" style="max-width: 18rem;">
                <textarea class="card-header" id="titleEl">{{title}}</textarea>
                <div class="card-body">
                    <ul style="text-decoration: underline !important; list-style-type: none !important; padding: 0 !important;">
                        <li><h5 class="card-title">{{book.title}}</h5></li>
                        <li><h5 class="card-title">{{book.author}}</h5></li>
                    </ul>
                    <h5 class="card-title">{{review.bookGenre}}</h5>
                    <textarea class="card-text" id="reviewEl">{{review}}</textarea>
                <div class="card-footer bg-transparent d-flex justify-content-between p-0">
                    <ul class="d-flex p-0" style="list-style-type:none;">
                        <li style="padding-top:5px; padding-right: 5px;">
                            <button type="button" class="btn btn-link editbtn" id="updatebtn" style="padding:10px;" value="{{id}}">Update</button>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div> --}}
</div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.js"></script>
    <script src="../js/update.js"></script>